
def area(a):
    if a <= 0:
        raise ValueError("Сторона квадрата не может быть отрицательной")
    ''' Принимает число a и возвращает его квадрат
    Пример: area(3) = 9 '''
    return a * a


def perimeter(a):
    if a <=0 0:
        raise ValueError("Сторона квадрата не может быть отрицательной")
    ''' Принимает число a и возвращает произведение числа a и 4
    Пример: perimeter(3) = 12 '''
    return 4 * a
